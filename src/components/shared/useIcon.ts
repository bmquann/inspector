import {
  HelpCircleOutline as help,
  NotificationsOutline as notifications,
  RefreshOutline as refresh,
  RepeatOutline as repeat
} from '@vicons/ionicons5'

export const icons = {
  refresh,
  help,
  notifications,
  repeat
}

export type IconTypes = keyof typeof icons
